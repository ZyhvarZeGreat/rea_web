import PageLoader from "@/components/common/Loader/PageLoader";
import React from "react";

type Props = {};

const Loading = (props: Props) => {
  return <PageLoader size="24" />;
};

export default Loading;
